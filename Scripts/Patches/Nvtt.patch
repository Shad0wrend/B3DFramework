diff --git a/CMakeLists.txt b/CMakeLists.txt
index 668cd7e..418abad 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -5,16 +5,9 @@ ENABLE_TESTING()
 SET(NV_CMAKE_DIR "${NV_SOURCE_DIR}/cmake")
 SET(CMAKE_MODULE_PATH ${CMAKE_MODULE_PATH} "${NV_CMAKE_DIR}")
 
-# Compiler check (needs -std:c++11 flag)
-include(CheckCXXCompilerFlag)
-CHECK_CXX_COMPILER_FLAG("-std=c++11" COMPILER_SUPPORTS_CXX11)
-if(COMPILER_SUPPORTS_CXX11)
-    set(CMAKE_CXX_STANDARD 11)
-    set(CMAKE_CXX_STANDARD_REQUIRED ON)
-    set(CMAKE_CXX_EXTENSIONS OFF)
-else()
-    message(FATAL_ERROR "The compiler ${CMAKE_CXX_COMPILER} has no C++11 support. Please use a different C++ compiler.")
-endif()
+set(CMAKE_CXX_STANDARD 11)
+set(CMAKE_CXX_STANDARD_REQUIRED ON)
+set(CMAKE_CXX_EXTENSIONS OFF)
 
 INCLUDE(${NV_CMAKE_DIR}/OptimalOptions.cmake)
 
diff --git a/extern/CMP_Core/source/cmp_math_func.h b/extern/CMP_Core/source/cmp_math_func.h
index c2f8908..6c4c494 100644
--- a/extern/CMP_Core/source/cmp_math_func.h
+++ b/extern/CMP_Core/source/cmp_math_func.h
@@ -33,6 +33,7 @@
 //============================================================================
 
 #include <algorithm>
+#include <cmath>
 using namespace std;
 
 static CGU_INT QSortFCmp(const void *Elem1, const void *Elem2) {
diff --git a/src/nvmath/nvmath.h b/src/nvmath/nvmath.h
index 2a4f887..2ce86b9 100644
--- a/src/nvmath/nvmath.h
+++ b/src/nvmath/nvmath.h
@@ -117,7 +117,8 @@ inline float asinf_assert(const float f)
 #define asinf asinf_assert
 #endif
 
-#if NV_CC_MSVC
+// MSVC 1800+ (VS 2013+) provides log2f / exp2f as intrinsics which cannot be redefined.
+#if NV_CC_MSVC && (_MSC_VER < 1800)
 NV_FORCEINLINE float log2f(float x)
 {
     nvCheck(x >= 0);
